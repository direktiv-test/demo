import { beforeAll, describe, expect, it } from '@jest/globals'

describe('Test send events v3 api', () => {
    it(`event by id`, async () => {
       return "JENS"
	})
    // it('should add up', 
    //     { xray: { testKey: 'TEST-123', requirements: ['REQ-1', 'REQ-2'] } }, // What I'd like to be able to add 
    //     () => { expect(1 + 2).toBe(3); }
    //  );
})
